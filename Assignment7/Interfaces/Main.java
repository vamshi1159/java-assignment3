package Assignment7.Interfaces;

public class Main {
    public static void main(String[] args) {
        Child child =new Child();
        child.interface1(child);
        child.interface2(child);
        child.interface3(child);
        child.combined(child);
    }
}
